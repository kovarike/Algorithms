{
    "projects": {
      "frontend": {
        "source": "apps/algorithms",
        "buildCommand": "npm run build",
        "outputDirectory": "apps/algorithms/.next"
      },
      "backend": {
        "source": "apps/engine",
        "routes": [
          { "src": "/api/(.*)", "dest": "/apps/engine/dist/index.js" }
        ]
      }
    }
  }
  